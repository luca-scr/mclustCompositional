% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compositional.R
\name{MclustCompositional}
\alias{MclustCompositional}
\alias{densityMclustCompositional}
\title{Fit Gaussian mixtures to compositional data}
\usage{
MclustCompositional(data, criterion = c("ICL", "BIC"), ...)

densityMclustCompositional(
  data,
  G = NULL,
  modelNames = NULL,
  criterion = c("BIC", "ICL"),
  prior = NULL,
  nstart = 25,
  parallel = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A numeric matrix, data frame, or vector of strictly positive
compositions. Observations are rows and compositional parts are columns.
Rows with missing values are omitted before fitting.}

\item{criterion}{Model-selection criterion. One of \code{"ICL"} or \code{"BIC"} for
\code{MclustCompositional()} and one of \code{"BIC"} or \code{"ICL"} for
\code{densityMclustCompositional()}.}

\item{...}{Additional arguments passed to \code{densityMclustCompositional()} or
to the internal mclust fitting step.}

\item{G}{Integer vector with the numbers of mixture components to consider.
Defaults to \code{1:9}, truncated to the available sample size.}

\item{modelNames}{Character vector of mclust covariance models. If \code{NULL},
univariate transformed data use \code{"E"} and \code{"V"}; multivariate transformed
data use \code{mclust.options("emModelNames")}, with high-dimensional safeguards.}

\item{prior}{Optional prior specification passed to the mclust fitting step.}

\item{nstart}{Number of random starts used by k-means initialization when
\code{G > 1}.}

\item{parallel}{Logical, numeric, character, or cluster object controlling
parallel model fitting. \code{FALSE} fits sequentially; \code{TRUE} uses all detected
cores; a number uses that many cores; \code{"snow"}/\code{"PSOCK"} or
\code{"multicore"}/\code{"FORK"} selects the cluster type.}

\item{seed}{Optional integer seed used for reproducible starts.}
}
\value{
\code{MclustCompositional()} returns an object of class
\code{"MclustCompositional"} and \code{"densityMclustCompositional"} containing the
selected model, posterior probabilities, classification, uncertainty,
entropy, and fitted parameters.

\code{densityMclustCompositional()} returns an object of class
\code{"densityMclustCompositional"} containing the selected density model and the
full BIC/ICL tables.
}
\description{
\code{MclustCompositional()} performs model-based clustering for compositional
data. Observations are transformed with isometric log-ratio (ILR) coordinates,
Gaussian mixture models are fitted with \code{mclust}, and densities are
evaluated on the original compositional scale using the transformation
Jacobian.
}
\details{
\code{densityMclustCompositional()} exposes the density-estimation interface used
by \code{MclustCompositional()}.

Compositional observations must be strictly positive. The closure constant is
arbitrary because the ILR transform is invariant to multiplication of a
composition by a positive scalar.
}
\examples{
set.seed(1)

y1 <- matrix(rnorm(80, mean = -2, sd = 0.25), ncol = 2)
y2 <- matrix(rnorm(80, mean =  2, sd = 0.25), ncol = 2)
x <- ilr_inverse(rbind(y1, y2))

fit <- MclustCompositional(
  x,
  G = 1:2,
  modelNames = "EEE",
  nstart = 1,
  seed = 10
)

fit$G
table(fit$classification)
head(predict(fit, what = "z"))

}
\seealso{
\code{\link[=ilr_transform]{ilr_transform()}}, \code{\link[=predict.densityMclustCompositional]{predict.densityMclustCompositional()}}
}
